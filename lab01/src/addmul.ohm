AddMul {
  Expr = Add                -- root

  Add = Add "+" Mul         -- plus
      | Mul                 -- mul

  Mul = Mul "*" Pri         -- times
      | Pri                 -- pri

  Pri = "(" Expr ")"        -- paren
      | number              -- num

  number = digit+           
}